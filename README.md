# TT4BH
**TT4BH** is a Go-based network enumeration tool.

## **Usage**

# First-time setup
`./testbuild --setup`

# Run enumeration
`./testbuild -t <target>`

# Passive-only enumeration
`./testbuild -t <target> --passive`
